{
  "name": "termdem",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "ready": "vp check && vp run -r test && vp run -r build",
    "test:smoke": "vp test run packages/termdem/tests/examples.smoke.test.ts --tagsFilter smoke",
    "dev": "vp run @akuederle/termdem#dev",
    "prepare": "vp config"
  },
  "devDependencies": {
    "agent-browser": "catalog:",
    "vite-plus": "catalog:"
  },
  "engines": {
    "node": ">=22.12.0"
  },
  "packageManager": "pnpm@10.33.1"
}
