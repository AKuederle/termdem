{
  "name": "@termdem-examples/git-vim",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "vp run @akuederle/termdem#build && vp exec tsc --noEmit",
    "createDemo": "vp run @akuederle/termdem#build && termdem record ./demo.tsx ./git-vim.webm",
    "previewDemo": "vp run @akuederle/termdem#build && termdem preview ./demo.tsx",
    "recordDemo": "vp run @akuederle/termdem#build && termdem record ./demo.tsx ./git-vim.webm"
  },
  "devDependencies": {
    "@akuederle/termdem": "workspace:*",
    "@types/node": "catalog:",
    "@types/react": "catalog:",
    "typescript": "catalog:"
  }
}
