{
  "name": "@termdem-examples/socket-cli",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "pnpm --filter @akuederle/termdem build && tsc --noEmit",
    "createDemo": "pnpm --filter @akuederle/termdem build && termdem record ./demo.tsx ./socket-cli.webm",
    "previewDemo": "pnpm --filter @akuederle/termdem build && termdem preview ./demo.tsx",
    "recordDemo": "pnpm --filter @akuederle/termdem build && termdem record ./demo.tsx ./socket-cli.webm"
  },
  "devDependencies": {
    "@akuederle/termdem": "workspace:*",
    "@types/node": "catalog:",
    "@types/react": "catalog:",
    "typescript": "catalog:"
  }
}
